{
  "name": "stellar-escape",
  "version": "1.0.0",
  "description": "Stellar Escape - A 5-level Phaser 3 platformer game",
  "scripts": {
    "start": "npx serve . -p 8080",
    "dev": "npx live-server --port=8080 --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "serve": "^14.0.0"
  }
}
